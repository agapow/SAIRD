class SequenceMutationHints < Hobo::ViewHints

	model_name "Resistance-associated mutation"
	
	field_help({
		:magnitude => "percent" 
	})
		
	field_help({
		:description => "Location and type of mutation, e.g. H275Y, 54Y. You
			should only enter a single mutation in this field, even if there
			is a mix of mutations at the same loci.",
		:magnitude => "If this mutation was only seen in a proportion of
		   sequences, how many percent? Enter this as a whole integer,
		   	e.g. 50, 75",
	})
	
end
